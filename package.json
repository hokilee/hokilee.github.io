{
  "name": "personal-web",
  "version": "1.0.0",
  "description": "Personal website with view counter server",
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

